extends ../../layouts/default-login-host

block head
  - var pageTitle = 'SpacePool - HOST My rental offers New Space Step 2'

block header
  -var headerClass = '_border'

block content
  +page-header._wide-lg._hpad
    .container
      +e.wrapper
        +page-header-side
          +link-icon('select-arrow')._no-shadow._back._bold.__back(href="#") Back to previous step
        +page-header-main._main_mgt
          +breadcrumbs-form
            +breadcrumbs(['Home', 'My offers', 'Create new Space']).__navigation.hide-md
            +e.step <b>Step 2</b> Title and Description
          +e.links
            +link-icon('save')(href="#")._no-shadow._icon_gray-dark._icon_pad30._bold.__link All changes saved
      +progress(28.6)
  +content-origin._wide-lg._reverse._last
    .container
      +e.wrapper
        +content-column-aside.hide-md
          +side-navigation('Fill the basic information about your Space. Provide short description and basic space rules.')._pt46
            +page-info.__info
              +page-info-image
                +icon('strings').icon_strings._grad2
              +page-info-title Space title and description
              +page-info-description Step 2 <i>of 7</i>
        +content-column-main._no-ptop._flex
          +form()._white._flex(data-validated-form)
            +e.container
              +form-section
                +grid.grid_big.grid_t55
                  +col({ xl: 12 })
                    +form-section-wrapper
                      +form-box('Space Name', '', '', '', '', '50')
                        +textarea(type="text" name="spaceName" placeholder="Space Name" rows="1" required maxlength="50").js-textarea-counter
                  +col({ xl: 12 })
                    +form-section-wrapper
                      +form-box('Space description', '', '', '', '', '400')
                        +textarea(type="text" name="spaceDescription" placeholder="Space description" rows="1" required maxlength="400").js-textarea-counter
              +form-section._mta
                +grid.grid_big.grid_t55
                  +col({ xl: 7, lg: 8, md: 12 })
                    +content-zone._mid2._pr
                      h5 Example for short and catchy Description
                      p The building comprises of private office space available to rent, studio offices, fixed desks, meeting rooms, a café, cinema room and a gym. Along with a Penthouse to sleep and plenty of terraces for the summer sun. Located close to Regents Street, just a short walk from Oxford Street and Oxford Circus tube itself.
                      p Our space in three words: <br> Bright, Creative, Service
                  +col({ xl: 5, lg: 4, md: 12 })
                    +form-section-wrapper._h100p._fxe
                      +button-icon('button', 'select-arrow')._short._wide.__button._mxw300._mb8(type="submit") Next


block panels
  +main-menu-inner
    +nav-list(getData('main-menu-user.json'))
