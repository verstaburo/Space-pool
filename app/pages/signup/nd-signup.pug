extends ../../layouts/redesign-default

block head
  - var pageTitle = 'Spacepool - Sign Up'

block bodyClasses
  - var isNoFooter = true

block header
  - var headerClass = ''
  - var pageClass = ''
  - var isMap = false
  - var host = false

block content
  +nd-section
    .nd-container.nd-container_centered
      +registration-form({title: 'Join Spacepool', registration: true,})

block panels

block popups
