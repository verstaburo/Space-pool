extends ../../layouts/redesign-default

block head
  - var pageTitle = 'Spacepool - Sign Up'

block bodyClasses
  - var isNoFooter = true

block header
  - var headerClass = ''
  - var pageClass = ''
  - var isMap = false
  - var host = false
  - var isNoUser = true

block content
  +nd-section
    .nd-container.nd-container_centered
      +registration-form-accready

block panels

block popups
