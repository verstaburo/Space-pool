extends ../../layouts/redesign-default

block head
  - var pageTitle = 'Spacepool - Log In'

block bodyClasses
  - var isNoFooter = true

block header
  - var headerClass = ''
  - var pageClass = ''
  - var isMap = false
  - var host = false
  - var isNoUser = true

block content
  +nd-section
    .nd-container.nd-container_centered
      +registration-form({title: 'Login', login: true})

block panels

block popups
