mixin nd-input()
  +b.INPUT.nd-input&attributes(attributes)

mixin nd-input-number(data)
  - var val = data.value || 0;
  - var addClass = data.readonly ? ' is-readonly' : '';
  +b.LABEL.nd-input-numberbox&attributes(attributes)(class=addClass)
    +e.field
      +e.label amount:
      if data.readonly
        +e.INPUT.input.js-numberbox-input(type="text" name=data.name value=val readonly)
      else
        +e.INPUT.input.js-numberbox-input(type="text" name=data.name value=val)
    +e.button.js-numberbox-minus
    +e.button.js-numberbox-plus

//- mixin input-numbers(name, placeholder, min, max)
//-   +b.input-numbers&attributes(attributes)
//-     - if (min && max) {
//-       +input.input-numbers__input.js-numberbox-input(type="text" name=name placeholder=placeholder data-min=min data-max=max)
//-     - } else if (max) {
//-       +input.input-numbers__input.js-numberbox-input(type="text" name=name placeholder=placeholder data-max=max)
//-     - } else if (min) {
//-       +input.input-numbers__input.js-numberbox-input(type="text" name=name placeholder=placeholder data-min=min)
//-     - } else {
//-       +input.input-numbers__input.js-numberbox-input(type="text" name=name placeholder=placeholder)
//-     - }
//-     +e.buttons
//-       +e.button.js-numberbox-minus
//-       +e.button.js-numberbox-plus

mixin nd-textarea()
  +b.TEXTAREA.nd-textarea&attributes(attributes)
    block

mixin nd-select()
  +b.SELECT.nd-select&attributes(attributes)
    block

mixin nd-checkbox(data)
  +b.LABEL.nd-checkbox&attributes(attributes)
    if data.checked
      if data.required
        +e.INPUT.input(name=data.name, value=data.value, checked, type="checkbox", required)
      else if data.checker
        +e.INPUT.input(name=data.name, value=data.value, checked, type="checkbox", data-checker=data.checker)
      else if data.switch
        +e.INPUT.input(name=data.name, value=data.value, checked, type="checkbox", data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, checked, type="checkbox")
    else if data.required
      if data.checker
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox", required, data-checker=data.checker)
      else if data.switch
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox", required, data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox", required)
    else
      if data.checker
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox", data-checker=data.checker)
      else if data.switch
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox", data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, type="checkbox")
    +e.SPAN.box
    if !data.nocontent
      +e.SPAN.content
        block

mixin nd-radio(data, tag)
  +b(tag || 'label').nd-checkradio&attributes(attributes)
    if data.checked
      if data.required
        +e.INPUT.input(name=data.name, value=data.value, checked, type="radio" required)
      else
        +e.INPUT.input(name=data.name, value=data.value, checked, type="radio")
    else if data.required
      +e.INPUT.input(name=data.name, value=data.value, type="radio" required)
    else
      +e.INPUT.input(name=data.name, value=data.value, type="radio")
    +e.SPAN.box
    if !data.nocontent
      +e.SPAN.content
        block

mixin radio-button(data, tag)
  +b(tag || 'label').radio-button&attributes(attributes)
    if data.checked
      if data.required
        +e.INPUT.input(name=data.name, value=data.value, checked, type="radio" required)
      else if data.switch
        +e.INPUT.input(name=data.name, value=data.value, checked, type="radio", data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, checked, type="radio")
    else if data.required
      if data.switch
        +e.INPUT.input(name=data.name, value=data.value, type="radio", required, data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, type="radio" required)
    else
      if data.switch
        +e.INPUT.input(name=data.name, value=data.value, type="radio", data-form-switch=data.switch)
      else
        +e.INPUT.input(name=data.name, value=data.value, type="radio")
    +e.wrapper
      +e.SPAN.box
      if !data.nocontent
        +e.SPAN.content
          block

//- mixin color-radio(name, value, checked)
//-   +b.LABEL.color-radio&attributes(attributes)
//-     - if (checked) {
//-       +e.INPUT.input(name=name, value=value, type="radio", checked)
//-     - } else {
//-       +e.INPUT.input(name=name, value=value, type="radio")
//-     - }
//-     +e.SPAN.content
//-       block

//- mixin button-radio(name, value, checked)
//-   +b.LABEL.button-radio&attributes(attributes)
//-     - if (checked) {
//-       +e.INPUT.input(name=name, value=value, type="radio", checked)
//-     - } else {
//-       +e.INPUT.input(name=name, value=value, type="radio")
//-     - }
//-     +e.SPAN.content
//-       +e.text
//-         block

//- mixin range(output)
//-   +b.range&attributes(attributes).js-range
//-     +e.slider(data-range-container)
//-     if output
//-       +e.output(data-range-output)
//-         +e.value_min Min. <span data-range-min></span> &pound;/mo
//-         +e.value_max Max. <span data-range-max></span> &pound;/mo
//-     block

//- mixin check-input(isChecked)
//-   +b.check-input&attributes(attributes)
//-     +checkbox('', '', isChecked).js-check-input-toggle
//-     +e.content
//-       +form-box('Price', '', '', '&pound;')._icon_left
//-         +input(type="text")
//-       if block
//-         +e.SPAN.text
//-           block
