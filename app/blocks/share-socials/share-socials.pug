mixin share-socials(data)
  +b.UL.share-socials&attributes(attributes)
    each social in data
      +e.LI.item
        +e.A.link(href=social.href)
          +e.IMG.icon(src=social.icon width="40" height="40" alt="@@")
    block
