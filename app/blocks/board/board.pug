mixin board()
  +b.SECTION.board&attributes(attributes)
    block

mixin board-header()
  +e.header&attributes(attributes)
    block

mixin board-content()
  +e.content&attributes(attributes)
    block

mixin board-title(tag)
  +e(tag || 'h3').title&attributes(attributes)
    block
