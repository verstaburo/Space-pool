mixin timer(name)
  +b.DIV.timer&attributes(attributes)
    +e.SPAN.name!= name
    +e.SPAN.counter
      +e.B.item 24hr
      +e.B.item 00min
    block

mixin timer-other(name)
  +b.DIV.timer&attributes(attributes)
    +e.SPAN.name!= name
    +e.SPAN.counter
      block
