mixin request-status(icon)
  +b.request-status&attributes(attributes)
    +e.mark
      +icon(icon)(class="icon_" + icon)
    +e.text
      block

mixin request-status-simple()
  +b.request-status_simple&attributes(attributes)
    +e.text
      block
