mixin time(start, startISO, end, endISO, color)
  - var addClass = ''
  -if (color) {addClass = '_color'}
  +b.time&attributes(attributes)
    +e.text From:
      +e.TIME.time(datetime=startISO)!= start
    +long-arrow.__divider
    +e.text(class=addClass) Till:
      +e.TIME.time(datetime=endISO)!= end
    block


mixin time-big(start, startISO, end, endISO, color)
  - var addClass = ''
  - if (color) {addClass = '_color'}
  +b.time_sb&attributes(attributes)
    +e.text From:
      +e.TIME.time(datetime=startISO)!= start
    +long-arrow._big.__divider
    +e.text(class=addClass) Till:
      +e.TIME.time(datetime=endISO)!= end
    block

mixin time-origin()
  +b.time_sb&attributes(attributes)
    block

mixin time-text()
  +e.text&attributes(attributes)
    block

mixin time-time()
  +e.TIME.time&attributes(attributes)
    block

mixin time-divider()
  +long-arrow.__divider&attributes(attributes)
